import React from 'react';

const Commencer = (props) => {
    return (
        <div className="commencer" onClick={props.fx}>
            <h4>COMMENCER ={'>'}</h4>
        </div>
    );
};

export default Commencer;